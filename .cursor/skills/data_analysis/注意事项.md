# 注意事项

存放 `data_analysis` 调用过程中发现的问题、筛选踩坑、口径歧义，以及人工追加的约定。

**维护规则**

- 每次调用若出现**本文件未收录**的问题，在对应分类下追加：日期、现象、原因（若已知）、规避写法。
- 已有条目不重复堆砌；内容过时可改「状态」为废弃并注明替代做法，勿直接删历史记录。
- 神舟 SQL 语法/分区类问题写入 sibling：`../hive_sql_create/神舟注意事项.md`，此处只留**交叉引用**或分析侧备注。

---

## 1. 看板取数

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| 2026-07-20 | 看板映射权威源 | 临时取数（看板）只读 `raw_data/看板说明/看板.csv`，勿再默认读 `app/AB-OCI/说明/看板.csv` |
| 2026-07-28 | 图片编辑四级功能看板 | `dashboardId=10015982`，chart `90318/90319/90314/90315/90311/90312`（进入/打勾/保存 UV·PV）；路由见 `分析思路.md` §2.0b；非全三级都有四级 |
| 2026-07-29 | OKR 完成度看板 | 订阅毛利：`10016076`/`10016079`；MAU/MNU：`10016080`/`10016081`（见 §2.0c）。勿与 `89060` 累计毛利混用 |
| 2026-07-20 | 订阅归因层级 | 权威：`raw_data/订阅归因层级/官方分级mapping.csv`（mapping.xlsx）；Eraser 图片路径为 Edit→Edit→Eraser→四级子项 |
| （模板） | 例：筛选名传错导致静默回默认 | 先 `include_response=false` 探 linkage，再用真实 `filters.name` |
| | 近 1/7/30 天留存为 0 | 见 `看板.csv` 备注：次留未出，勿当真实跌 0 |
| | 2026 订阅毛利用错图 | 日毛利用 `89046`；OKR/累计进度用 `89060`（累计≠日毛利加总） |
| | 订阅链路近 7 天付费偏低 | `10015756` 等为触发日归因，近端付费未完全回流 |

（新增条目写在本表下方或本分类续行。）

## 2. 神舟 / SQL

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| 2026-07-20 | 埋点事件已入库 | 权威：`raw_data/埋点事件/`；神舟优先 `事件知识_检索.csv`；触发「更新事件埋点」跑 `scripts/update_event_tracking.py` |
| 2026-07-20 | 原 xlsx 合并单元格 | 整理须按「事件id 非空」分块回填，禁止整列 ffill（会串事件名） |
| 2026-07-20 | 大禹 versions 无效版本 | OCI Airbrush：不存在的版本号查询会返回**全部**需求；须用 `0<total<全量` 过滤真实版本 |
| 2026-07-20 | 更新埋点版本水位 | `update_state.json.last_synced_version`；CF 版本源 `知识库/.../site_632691935`；手动版每次必合并 |
| 2026-07-20 | 神舟先确认再跑 | 写完 SQL 须给用户看一眼，明确确认后才 `run --wait --download` |
| 2026-07-20 | 事件口径未命中先问 | 埋点知识无对应 event/key/值时先询问用户，禁止编造后直接写 SQL |
| 2026-07-20 | 北斗 `ALL_DAY` 聚合枚举不一致 | dashboard linkageConfig 可能返回 `ALL_DAY`，但当前 `beidou_tool_api.py` 仅接受 DAY/WEEK/BIWEEK/MONTH/QUARTER/YEAR；需保留日粒度结果后本地求和，并注明时间范围 |
| | 详见 `hive_sql_create/神舟注意事项.md` | 分区字面量、JOIN 两侧子查询、慎用 WITH、多表列加别名等 |
| | `--project` 报错 | 调 `projects --env oci` 列出候选，勿换环境盲试 |

## 3. 周报 / 月报

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| | 知识库路径混用 | 只用 `知识库` skill 的 `wiki/`，勿用旧路径 `app/AB-OCI/专项/AI周报/wiki/` |
| | 只要 v1 却跑完全部 | 听用户触发词；默认完整流水线以各 skill 为准 |

## 4. 归因 / 知识库

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| | 节假日年份错位 | 按分析周期年份查 `airbrush-holiday-calendar` |
| | 有 mark 仍写产品猜测 | 匹配优先级：marks > 历史周报 > 节假日 > 产品需求 |

## 5. 实验 / 专题

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| | 冲突指标目录混淆 | `ab-experiment-conflict`：是否限制新用户决定 `所有用户` / `新用户` 子目录 |
| | | |

## 6. 环境与鉴权

| 日期 | 现象 | 规避 / 约定 |
|------|------|-------------|
| | 北斗/神舟 401 或无 token | 配置 `OMNIBUS_ACCESS_TOKEN`；鉴权失败再提示，正常执行不贴长教程 |
| | 网关不可达 | 不跨环境兜底；提示在可访问 connectors 的网络执行 |

## 7. 其他（手动追加）

<!-- 自由格式：会议约定、临时口径、待办等 -->
